# AGENTS.md

## Cursor Cloud specific instructions

This repository (`auto_agent_test`) is currently an empty/skeleton project with no source code, dependencies, or services. There is nothing to build, lint, test, or run.

- **No package manager or dependency files** exist (no `package.json`, `requirements.txt`, `go.mod`, etc.).
- **No services** need to be started.
- **No tests** exist to run.

When source code is added to this repository, update this section with relevant build/run/test instructions.
